//This controller is a stub for the history page

var historyController = function($scope, $log, appInfo,youtubeFactory) {
    $scope.appInfo = appInfo;
};

myTubeApp.controller('historyController', historyController);