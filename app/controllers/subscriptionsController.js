//This controller is a stub for the subscriptions page

var subscriptionsController = function($scope, $log, appInfo,youtubeFactory) {
    $scope.appInfo = appInfo;
};

myTubeApp.controller('subscriptionsController', subscriptionsController);